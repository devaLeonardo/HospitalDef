@model IEnumerable<HospitalDef.Models.Recetum>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Observaciones)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Tratamientos)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Diagnostico)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FolioCitaNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Observaciones)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Tratamientos)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Diagnostico)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FolioCitaNavigation.FolioCitas)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.FolioReceta">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.FolioReceta">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.FolioReceta">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
